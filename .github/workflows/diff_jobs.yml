name: Multiple jobs

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Building Application"
      
  test:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - run:  Running Tests

  deploy:
     if: always()
     needs: test
     runs-on: ubuntu-latest
     steps:
       - run: echo "deploying application"
       

  
